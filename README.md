# WeatherLighting_renderSettings
Scripts utilizzati nell'applicazione in unity per un midterm in università. questi file sono quelli utilizzatti per fare da ponte tra OpenWeather e l'applicazione che utilizza MQTT con broker mosquitto.
